[nosetests]
logging-format = %8(levelname)s %(pathname)s:%(funcName)s:%(lineno)d: %(message)s

verbosity = 1

no-byte-compile = 1

with-coverage = 1
cover-package = srforge
cover-erase = 1

with-doctest = 1
doctest-tests = 1
